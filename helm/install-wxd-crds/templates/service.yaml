apiVersion: v1
kind: Service
metadata:
  name: {{ include "install-wxd-crds.fullname" . }}
  labels:
    {{- include "install-wxd-crds.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "install-wxd-crds.selectorLabels" . | nindent 4 }}
