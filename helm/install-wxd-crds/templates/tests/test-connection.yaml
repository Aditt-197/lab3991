apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "install-wxd-crds.fullname" . }}-test-connection"
  labels:
    {{- include "install-wxd-crds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "install-wxd-crds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
