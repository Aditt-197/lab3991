apiVersion: v1
kind: Service
metadata:
  name: {{ include "install-wxd.fullname" . }}
  labels:
    {{- include "install-wxd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "install-wxd.selectorLabels" . | nindent 4 }}
