apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "install-software-hub-crds.fullname" . }}-test-connection"
  labels:
    {{- include "install-software-hub-crds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "install-software-hub-crds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
