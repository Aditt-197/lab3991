apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: deploy-software-hub-application
  namespace: openshift-gitops
spec:
  project: default

  source:
    repoURL: https://github.com/Aditt-197/lab3991.git
    targetRevision: main
    path: helm/install-software-hub
    helm:
      releaseName: software-hub

  destination:
    server: https://kubernetes.default.svc
    namespace: cpd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true